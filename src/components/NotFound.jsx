import React from 'react';

const NotFound = (props) => {
    return (
        <div>
            <h1>These aren't the droids you're looking for" with a picture of Obi-Wan Kenobi.</h1>
        </div>
    );
};

export default NotFound;